const WIDTH = 1300 // from svg image
const CENTER = 649 // 1300/2 - 1
const UNIT_RADIUS = 547 // determined experimentally
const OUTER_RADIUS = 640 // determined experimentally and slightly outside chart

export {
  CENTER,
  OUTER_RADIUS,
  UNIT_RADIUS,
  WIDTH
}
